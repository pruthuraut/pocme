// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Disable React Compiler prompt (no effect in CLI)
  // Future flags can be added here
};
module.exports = nextConfig;
